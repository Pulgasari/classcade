// @classcade/presets/tokens.js

// design tokens the presets draw from. kept separate so a consumer can swap
// them without touching the preset logic.

export const breakpoints = {
  sm : '(min-width: 40rem)',
  md : '(min-width: 48rem)',
  lg : '(min-width: 64rem)',
  xl : '(min-width: 80rem)',
};
